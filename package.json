{
    "name": "fullstack-app",
    "version": "1.0.0",
    "scripts": {
        "start": "cd backend && node index.js",
        "heroku-postbuild": "cd frontend && npm install && npm run build"
    },
    "engines": {
        "node": "18.x"
    }
}